{
  "name": "flexy-components",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "scripts": {
    "esbuild:start": "ENV=development node esbuild.js",
    "esbuild:build": "ENV=production node esbuild.js",
    "hugo:start": "hugo server -s docs --gc --disableFastRender --bind 0.0.0.0 --port 8000 --noHTTPCache --baseURL 'http://localhost:8000/'",
    "hugo:build": "rm -fr public/* && hugo -s docs --gc",
    "build": "npm run esbuild:build && npm run hugo:build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.25.9",
    "esbuild-sass-plugin": "^3.3.1",
    "prettier": "^3.6.2",
    "prettier-plugin-go-template": "^0.0.15",
    "sass": "^1.92.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@nureon22/ripple-effect": "^1.7.0"
  }
}
