{
  "name": "flexy-components",
  "version": "0.1.1",
  "description": "",
  "type": "module",
  "main": "dist/esm/main.js",
  "types": "dist/types/main.d.ts",
  "exports": {
    ".": "./dist/esm/main.js",
    "./styles": "./dist/esm/main.css"
  },
  "scripts": {
    "esbuild:start": "ENV=development node esbuild.js",
    "esbuild:build": "ENV=production node esbuild.js",
    "hugo:start": "hugo server -s docs --gc --disableFastRender --bind 0.0.0.0 --port 8000 --noHTTPCache --baseURL 'http://localhost:8000/'",
    "hugo:build": "rm -fr docs/public/* && hugo -s docs --gc --minify",
    "build:types": "npx tsc --outDir dist/types",
    "build": "npm run esbuild:build && npm run build:types"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "repository": {
    "url": "https://github.com/nureon22/flexy-components"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "esbuild-sass-plugin": "^3.3.1",
    "prettier": "^3.6.2",
    "prettier-plugin-go-template": "^0.0.15",
    "sass": "^1.92.0",
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@nureon22/ripple-effect": "^1.7.0"
  }
}
